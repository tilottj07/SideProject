@model SchedulerApp.Models.Schedule.EditSchedule
<!-- Modal -->
<div class="modal fade" id="add-schedule" role="dialog" aria-labelledby="addScheduleLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <span class="modal-title" id="addScheduleLabel">@Model.ModalTitle</span>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <div class="modal-body">
                <form asp-action="EditScheduleModal">

                    @Html.HiddenFor(x => x.ScheduleId)
                    @Html.HiddenFor(x => x.UserIdPlaceholder)
                    <input name="IsValid" type="hidden" value="@ViewData.ModelState.IsValid.ToString()" />

                    <div class="form-group">
                        <label asp-for="StartDate"></label>
                        <input asp-for="StartDate" class="form-control" />
                        <span asp-validation-for="StartDate" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="EndDate"></label>
                        <input asp-for="EndDate" class="form-control" />
                        <span asp-validation-for="EndDate" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="TeamId"></label>
                        <select class="form-control" style="width: 100%" asp-for="TeamId"
                                asp-items="Model.TeamSelectList"></select>
                        <span asp-validation-for="TeamId" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="UserId"></label>
                        <select class="form-control" style="width: 100%" asp-for="UserId"
                                asp-items="Model.UserSelectList"></select>
                        <span asp-validation-for="UserId" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="SupportLevel"></label>
                        <select class="form-control" style="width: 100%" asp-for="SupportLevel"
                                asp-items="Model.SupportLevelSelectList"></select>
                        <span asp-validation-for="SupportLevel" class="text-danger"></span>
                    </div>           

                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-primary" data-save="modal">Save</button>
            </div>
        </div>
    </div>
</div>